# CLAUDE.md — きゅうり生育予測ノート（摘心栽培）

施設キュウリの摘心栽培向けに、生育予測・かん水量・液肥量を計算するWebアプリ。
GitHub Pages で公開する静的サイトで、ビルドは行わない。

作業フォルダは `D:\AI_claude\cucumber-fertilizer-planner\`（GitHub リポジトリ `y-ookuma/cucumber-fertilizer-planner` の複製）。
`main` ブランチに push すると GitHub Pages に反映される。

## ファイル構成

| ファイル | 内容 |
|---|---|
| `index.html` | アプリ本体（HTML・CSS・JSを1ファイルに収めている。約500KB） |
| `README.md` | 利用者向け説明書。**本文の正本** |
| `document/施設きゅうりの摘心栽培における生育予測に基づいた栽培管理導入マニュアル.pdf` | 計算方法の根拠となるマニュアル（表2-1, 2-3, 2-7, 5-1, 図4-2 など） |
| `document/回帰分析の考え方*.html` | 回帰分析の解説ページ |
| `document/生育調査シート.pdf` | 調査用紙 |
| `privacy.html` / `terms.html` / `icon` / `LICENSE` | 公開用の付属ファイル |

## アーキテクチャの要点

- データは `state` オブジェクトにまとめ、`saveState()` で window.storage → localStorage に保存する。
- Google Sheets 同期は GIS の OAuth トークン（約1時間）を使う。`push()` / `pull()` が送受信する。
- 外気象は Open-Meteo API から取得する。
- 変更の検知は `changeSeq` と `state.auth.dirty` / `dirtyAt` で行い、自動同期は3秒のデバウンスで `push(true)` を呼ぶ。
- スプレッドシートは9シート：`SHEET_ORDER`、`SH`（列定義）、`SHKEY` で定義している。

## 守るべき設計ルール（決定済み）

1. **時刻はJSTのみを扱う。** UTCのデータは9時間ずれて昼温・夜温・DIFが崩れる。READMEにも明記済み。
2. **DIFの向きは次の定義に統一する。**
   - DIF大（昼温−夜温の差が大きい）＝節間が伸びる＝栄養成長。
   - DIF小・マイナス＝生殖成長。
   - 仕組みはGA・IAAの温度応答と、夜間の呼吸抑制による同化産物の蓄積。
   - DIFは草姿を決める要因で、花芽形成とは別。
3. **いまの側の判定は `dev`（平準点より上か下か）の符号で行う。**
   - 相関の符号が変わる日数（茎16日・節間12日）は「平準点を通過する日」。
   - 負の相関が最大になる日数（32日・24日）は「反対側のピーク」。
4. **管理強度（マニュアル図4-2）**
   - 平準点から離れていく局面＝強、戻る局面＝弱。
   - 振れ幅＝|dev|：小 <0.34、中 <0.67、大 それ以上（`SWING_MID` / `SWING_BIG`）。
5. **かん水係数はマニュアル表2-3の範囲内にする。** 現在の値は 160/140/110/90/70。
6. LAIは計算に使っていない。
7. 外気象で補った値には `wxf` フラグを付け、画面では `外` マーク（`WX_MARK`）で示す。回帰では白抜きの点として区別する。
8. 回帰分析のYは「日次収量」を推奨する。持出養分は少なめに出る旨の注記がある（`YIELD_REG_WARN` / `NUTRIENT_SCOPE_NOTE`）。
9. **収量は毎日の記録の合計を優先する**（`effectiveYield`）。
   - 生育調査の手入力値は、その期間に毎日の記録の収量が1日も無いときだけ使う。
   - 見込み収量（`npkYieldSuggestion`）は毎日の記録の直近14日（`YIELD_SUGGEST_DAYS`）から1日あたりの収量を出す。毎日の記録が無ければ生育調査の直近2回分を使う。
10. データを失う操作（同日の記録の上書き、各種の削除、未送信の変更がある状態での取り込み）には確認ダイアログを出す。

マニュアルと食い違う変更をするときは、先に根拠（表・図の番号）を示して確認をとる。

## 実装上の約束

- **`saveState(false)`**：ユーザーデータ以外の変更（ハウスの切替、認証の設定、push/pull後の保存など）で使う。未送信フラグを立てないためのもの。
- **シートや列を追加するとき**は次をそろえて直す：
  - `SHEET_ORDER` / `SH` / `SHKEY`
  - `cellIn` の型変換
  - `push` / `pull`
  - アプリ内のシート構成カード
  - README 6章
- **README を編集したら、`index.html` 内の `const README_MD = "...";` も同じ内容に更新する。**
  - これは `file://` で開いたときの予備。通常は `README.md` を fetch して表示する。
  - 更新手順：README.md の中身を `\` → `\\`、`"` → `\"`、改行 → `\n` の順でエスケープし、その1行を置き換える。
- コードの書き方は周囲に合わせる（1ファイル構成、短い関数名、コメントは少なめ）。
- UIの文言と README は日本語。README の章番号を本文中で参照しない（章の構成が変わるとずれるため）。

## 動作確認

- Node.js は入っていない。JSの確認は **headless Edge** で行う。
  - `msedge --headless=new --disable-gpu --virtual-time-budget=8000 --dump-dom file:///...` を使う（`--user-data-dir` を付けると出力が空になることがある）。
  - テスト用スクリプトを差し込んだコピーをスクラッチパッドに作り、エラーや結果をDOMに書き出して読む。
- Googleサインインの実際の流れはローカルでは試せない。未確認であることを報告する。
- PDFの本文は Python の pypdf で取り出す。

## 環境（Windows / PowerShell 5.1）

- `&&` は使えない。`;` と `if ($?)` を使う。
- Git は `C:\Program Files\Git\cmd\git.exe`、GitHub CLI は `gh`。PATH が通っていないシェルではフルパスで呼ぶ。
- D: ドライブは所有者を記録しないため、`safe.directory` に登録済み。
- ZIPを作るときは `Compress-Archive` ではなく .NET の `ZipArchive` を使う。
  - パス区切りは `/`、ファイル名はUTF-8にする（Macでの文字化けやフォルダ崩れを防ぐため）。

## Git の運用

- push の前に `git fetch` で GitHub 側の変更を確かめる。GitHub の Web 画面から直接更新することもあるため。
- force push はしない。
- コミットメッセージは日本語で、何をなぜ変えたかを1行目に書く。

## 未着手の改善候補

- 花数・果実数を株あたりで保存する
- .ics の UID を再現性のあるものにする
- 計算のメモ化（パフォーマンス）
- XSS対策の強化

# CLAUDE.md — きゅうり生育予測ノート（摘心栽培）

施設キュウリの摘心栽培向けに、生育予測・かん水量・液肥量を計算するWebアプリ。
GitHub Pages で公開する静的サイトで、ビルドは行わない。

作業フォルダは `D:\AI_claude\cucumber-fertilizer-planner\`（GitHub リポジトリ `y-ookuma/cucumber-fertilizer-planner` の複製）。
`main` ブランチに push すると GitHub Pages に反映される。

## ファイル構成

| ファイル | 内容 |
|---|---|
| `index.html` | アプリ本体（HTML・CSS・JSを1ファイルに収めている。約500KB） |
| `README.md` | 利用者向け説明書。**本文の正本** |
| `document/施設きゅうりの摘心栽培における生育予測に基づいた栽培管理導入マニュアル.pdf` | 計算方法の根拠となるマニュアル（表2-1, 2-3, 2-7, 5-1, 図4-2 など） |
| `document/回帰分析の考え方*.html` | 回帰分析の解説ページ |
| `document/生育調査シート.pdf` | 調査用紙 |
| `document/Blanco_Folegatti2005_*.pdf` | 参照論文（CC BY-NC 4.0 のためリポジトリに収録） |
| `document/論文とマニュアルの突き合わせとアプリの対応表.pdf` | 東出ら2012とマニュアルの突き合わせ・アプリの対応状況（自作。アプリの対応が変わったら作り直す） |
| `document/マニュアルの食い違いとアプリの対応表.pdf` | マニュアル内の食い違い8項目とアプリの採り方（自作。対応が変わったら作り直す） |
| `document/東出ら2012_*.pdf`・`document/安ら2015_*.pdf` | 参照論文。再配布の許可がないため `.gitignore` で除外し、手元だけに置く。GitHub に上げない |
| `privacy.html` / `terms.html` / `icon` / `LICENSE` | 公開用の付属ファイル |

## アーキテクチャの要点

- データは `state` オブジェクトにまとめ、`saveState()` で window.storage → IndexedDB（DB `kyuuri`・ストア `kv`）→ localStorage の順に保存する。
  - localStorage は約5MBで上限に達したため IndexedDB に移した。`loadState()` は保存時刻 `at` を比べて新しい方を読み、localStorage にしか無いデータは IndexedDB へ移してから消す。
  - 起動時に `navigator.storage.persist()` で永続保存を依頼する。
  - 保存中（`savePending>0`）と保存失敗中（`saveFailed`）は、`beforeunload` でタブを閉じる前の確認を出す。
- Google Sheets 同期は GIS の OAuth トークン（約1時間）を使う。`push()` / `pull()` が送受信する。
- 外気象は Open-Meteo API から取得する。
- 変更の検知は `changeSeq` と `state.auth.dirty` / `dirtyAt` で行い、自動同期は3秒のデバウンスで `push(true)` を呼ぶ。
- スプレッドシートは9シート：`SHEET_ORDER`、`SH`（列定義）、`SHKEY` で定義している。

## 守るべき設計ルール（決定済み）

1. **時刻はJSTのみを扱う。** UTCのデータは9時間ずれて昼温・夜温・DIFが崩れる。READMEにも明記済み。
2. **DIFの向きは次の定義に統一する**（2026-09-24 に以前の定義から変更）。
   - プラスDIF（昼高夜低）＝節間伸長・果実肥大＝生殖成長優勢。
   - DIF短縮・マイナスDIF（昼夜差縮小）＝節間伸長抑制・樹勢維持＝栄養成長へのシフト。
   - マニュアルの表2-7・図4-1と同じ向きで、本文p.15とは逆。
   - 節間長の読み方（長い＝栄養、短い＝生殖。表3-1）は変えない。DIFを大きくした直後は節間長が伸びて栄養側に見えることがあると注記する。
3. **いまの側の判定は `dev`（平準点より上か下か）の符号で行う。**
   - 相関の符号が変わる日数（茎16日・節間12日）は「平準点を通過する日」。
   - 負の相関が最大になる日数（32日・24日）は「反対側のピーク」。
   - 自分のハウスの周期の目安（`ownCycle`・カード `cardCycle`）：補間しない実測値を直線で傾き除去し、三角形の重み（±`CYC_WIN`=7日、差0日は r=1）で日数ごとの自己相関を出し、正→負の日数を「平準点を通過」、その後の最小を「反対側のピーク」とする。調査10回以上・期間42日以上で表示。**予測（`analyse`）はマニュアルの16日・12日のまま**で、推定値は確認用。
4. **管理強度（マニュアル図4-2）**
   - 平準点から離れていく局面＝強、戻る局面＝弱。
   - 振れ幅＝|dev|：小 <0.34、中 <0.67、大 それ以上（`SWING_MID` / `SWING_BIG`）。
5. **かん水係数はマニュアル表2-3の範囲内にする。** 現在の値は 160/140/110/90/70。
   - 液肥のN/K比は表2-5の目安 0.6〜1.5（`NK_MIN`／`NK_MAX`）を外れたら「目安の範囲外」と表示する（`nkOut`・`nkCell`）。候補の選び方（`fertPick`）は変えず、表示と注意書きで知らせる。
6. **LAIは東出ら（2012）にもとづいて扱う。**
   - 受光率 f = 1 − e^(−1.17・LAI)（`LAI_K`）。適正の目安 1.5〜2.5（`LAI_LOW` / `LAI_HIGH`、論文の「LAI 2で約90%」から決めた本アプリの値）。
   - 調査記録の LAI を日ごとに線形補間（`laiSeries`、定植日は 0）。LAI の記録があれば作物係数は `kcForLai`（LAI 2.0 で 1.0）、なければ `kcForDays`。
   - NARO式（安ら 2015 野菜茶研報14）：RLA(n)=0.896/(1+14.811e^(−0.51n))、株葉面積＝∫₀ᴺRLA×最大葉面積（`rlaIntegral`・`laiFromLeaves`）。最大葉面積は Blanco・Folegatti 2005 の 0.88LW−4.27（cm²）。「摘心後」モード（葉数×0.896×最大葉）は本アプリの簡略化で論文の検証外。葉の測定は株ごとに入力し（`leafGrid`、株1〜6）、株ごとの値を `lcModeP`・`lcNP`・`lcLP`・`lcWP`（株の位置を保つ配列、空欄は null）に保存する。LAI欄には各株LAIの平均を入れる。`lcN`・`lcL`・`lcW` は3項目がそろった株の平均、`lcMode` は grow／pinched／mixed（以前の版との互換とCSV用）。
   - 光から見た収量の目安（第3段階）：見込み収量＝Σ受光量×LUE（既定0.85、幅0.69〜1.00）×果実分配率（定植40日0.56→98日0.79を直線、`fracAuto`）÷果実乾物率（`npkDM`、既定5.0%）。LUE・分配率の手入力は `state.lightModel`（端末のみ、シート同期なし）。
   - 果実乾物率の既定は 5.0%（東出ら 2012 の実測。以前は 4.0%）。
   - ハウスに被覆資材（`cover`、`COVER_GROUPS` から選択または手入力）・被覆の層数（`layers`＝一重／二重／三重以上）・遮光カーテン（`shade`＝なし／あり）・遮光率（`shadeRate`、%手入力）・実測の透過率（`tauManual`、%）を登録できる。
   - 透過率 `houseTau`＝1層の目安（`COVER_TAU`）^層数 × `TAU_FRAME`(0.85)。実測値があれば優先。遮光は屋外日射 `SHADE_ON_WM2`(800W/m²) 以上の時刻だけ（1−遮光率）を掛ける。目安の数値は本アプリの仮置きで、論文由来ではない。
   - `lightInHouse` が外気由来（`wxf`）の r・rdir・rdif・par にだけ透過率を掛ける。保存値は屋外のまま、`envDaily` の集計時に換算する。日射比例かん水（外気象の予報 `wxCache`）は対象外。
   - 受光量 `ipar`＝受光率（補間LAI）×積算PAR。環境表の列・日計CSV・回帰分析のXに追加。
7. 外気象で補った値には `wxf` フラグを付け、画面では `外` マーク（`WX_MARK`）で示す。回帰では白抜きの点として区別する。
8. 回帰分析のYは「日次収量」を推奨する。持出養分は少なめに出る旨の注記がある（`YIELD_REG_WARN` / `NUTRIENT_SCOPE_NOTE`）。
9. **収量は毎日の記録の合計を優先する**（`effectiveYield`）。
   - 生育調査の手入力値は、その期間に毎日の記録の収量が1日も無いときだけ使う。
   - 見込み収量（`npkYieldSuggestion`）は毎日の記録の直近14日（`YIELD_SUGGEST_DAYS`）から1日あたりの収量を出す。毎日の記録が無ければ生育調査の直近2回分を使う。
10. **開花数・着果数は株ごとに保存する**（`flowerP`・`fruitP`、株の位置を保つ配列）。`flower`・`fruit` は合計として残す。
    - `fruitStats` が1株あたり開花数・着果数と着果率（両方を入れた株の着果数合計÷開花数合計）を出す。
    - 株ごとの値がない以前の記録は、合計÷測定株数（茎径か節間長の株数）を近似とし `est:true` で表示に「近似」を付ける。
    - シートでは株ごとの配列を空白区切り、空欄は `-` で書き出す（`cellOut`／`PLANT_COLS`）。
11. データを失う操作（同日の記録の上書き、各種の削除、未送信の変更がある状態での取り込み）には確認ダイアログを出す。

マニュアルと食い違う変更をするときは、先に根拠（表・図の番号）を示して確認をとる。

## 実装上の約束

- **`saveState(false)`**：ユーザーデータ以外の変更（ハウスの切替、認証の設定、push/pull後の保存など）で使う。未送信フラグを立てないためのもの。
- **シートや列を追加するとき**は次をそろえて直す：
  - `SHEET_ORDER` / `SH` / `SHKEY`
  - `cellIn` の型変換
  - `push` / `pull`
  - アプリ内のシート構成カード
  - README 6章
- **README を編集したら、`index.html` 内の `const README_MD = "...";` も同じ内容に更新する。**
  - これは `file://` で開いたときの予備。通常は `README.md` を fetch して表示する。
  - 更新手順：README.md の中身を `\` → `\\`、`"` → `\"`、改行 → `\n` の順でエスケープし、その1行を置き換える。
- コードの書き方は周囲に合わせる（1ファイル構成、短い関数名、コメントは少なめ）。
- UIの文言と README は日本語。README の章番号を本文中で参照しない（章の構成が変わるとずれるため）。

## 動作確認

- Node.js は入っていない。JSの確認は **headless Edge** で行う。
  - `msedge --headless=new --disable-gpu --virtual-time-budget=8000 --dump-dom file:///...` を使う（`--user-data-dir` を付けると出力が空になることがある）。
  - テスト用スクリプトを差し込んだコピーをスクラッチパッドに作り、エラーや結果をDOMに書き出して読む。
  - IndexedDB など実I/Oを伴う非同期処理は `--dump-dom` では完了前に書き出される。その場合はローカルHTTPサーバーでページを開き、結果を POST で受け取る方法をとる。
  - テストで起動した Edge は PID を指定して終了する（`taskkill /IM msedge.exe` は利用者のブラウザまで閉じるので使わない）。
- Googleサインインの実際の流れはローカルでは試せない。未確認であることを報告する。
- PDFの本文は Python の pypdf で取り出す。

## 環境（Windows / PowerShell 5.1）

- `&&` は使えない。`;` と `if ($?)` を使う。
- Git は `C:\Program Files\Git\cmd\git.exe`、GitHub CLI は `gh`。PATH が通っていないシェルではフルパスで呼ぶ。
- D: ドライブは所有者を記録しないため、`safe.directory` に登録済み。
- ZIPを作るときは `Compress-Archive` ではなく .NET の `ZipArchive` を使う。
  - パス区切りは `/`、ファイル名はUTF-8にする（Macでの文字化けやフォルダ崩れを防ぐため）。

## Git の運用

- push の前に `git fetch` で GitHub 側の変更を確かめる。GitHub の Web 画面から直接更新することもあるため。
- force push はしない。
- コミットメッセージは日本語で、何をなぜ変えたかを1行目に書く。

## 未着手の改善候補

- 節あたり着果数（東出ら 2012 の収量構成要素。節数の記録が必要）
- .ics の UID を再現性のあるものにする
- 計算のメモ化（パフォーマンス）
- XSS対策の強化
